version: '3.8'

services:
  redis:
    image: redis:7
    ports:
      - "6379:6379"
    command: >
      redis-server --maxmemory 50mb --maxmemory-policy allkeys-lfu

  app_python:
    build: .
    depends_on:
      - redis
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      - .:/app
